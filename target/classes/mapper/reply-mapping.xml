<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reply">

	<!-- 댓글 입력 -->
	<insert id="insertReply">
		INSERT INTO REPLY(rno, seq, replytext, replyer)
		VALUES (reply_seq.NEXTVAL, #{seq}, #{replytext}, #{replyer});
	</insert>
	
	<!-- 댓글 목록 -->
	<select id="listReply" resultType="com.springbook.biz.board.reply.ReplyVO">
		SELECT r.rno, r.seq, r.replytext, r.replyer, u.uname, r.regdate,r.updatedate
		FROM REPLY r, user u
		where r.replyer= u.uname AND r.seq=#{seq}
		ORDER BY rno
		
	</select>

</mapper>